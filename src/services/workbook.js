import { supabase } from '../lib/supabase.js';

// TODO Phase 4: Load the weekly_entries row for the given Monday date (ISO string)
export async function getWeeklyEntry(weekStart) {}

// TODO Phase 4: Upsert a weekly_entries row for the given Monday date
export async function saveWeeklyEntry(weekStart, fields) {}
