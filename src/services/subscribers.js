import { supabase } from '../lib/supabase.js';

// TODO Phase 5: Insert a row into subscribers (no auth required)
// Called by the /api/subscribe Netlify Function, not directly from the browser
export async function addSubscriber(email, name, interest, source) {}
