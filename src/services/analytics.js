import { supabase } from '../lib/supabase.js';

// TODO Phase 5: Insert a row into page_events (no auth required)
// Will replace / extend the current navigator.sendBeacon path in mindearn-app.js
export async function trackEvent(eventName, detail, page) {}
